services:
  knowledge-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ~/anvil-workspace:/workspace:ro
    env_file:
      - .env
    environment:
      - KNOWLEDGE_REPO=akhera/knowledge-base
      - SYNC_INTERVAL=300
    restart: unless-stopped
