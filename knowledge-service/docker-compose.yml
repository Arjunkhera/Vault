services:
  knowledge-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ~/anvil-workspace:/workspace:ro
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - KNOWLEDGE_REPO=fdp-docmgmt/knowledge-base
      - SYNC_INTERVAL=300
    restart: unless-stopped
